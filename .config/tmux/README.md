# Tmux config

- minimalistic tmux config that displays your local & public IP over statusline
- rebinds prefix to `C-t` instead of `C-b` default
- sets vim-like bindings for pane switching

## Installation

- Remember to run `tmux source-file ~/.tmux.conf` to load the config
